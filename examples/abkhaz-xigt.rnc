include "xigt.rnc" {
Tier |= grammar { include "xigt-tier.rnc" {
    Tier.type = "syntax"
    Elem |= grammar { include "xigt-elem.rnc" {
        Elem.ref = notAllowed
        Elem.content = attribute synref { AlgnExpr }
    } }
} }
}
